services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8000}
    image: frontend-crawler:latest
    container_name: crawler-frontend
    ports:
      - "8080:80"
